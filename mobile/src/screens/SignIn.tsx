import { NativeBaseProvider, Text, Center } from "native-base";

export function SignIn() {
  return (
    <NativeBaseProvider>
      <Center flex={1} bgColor="gray.900"></Center>
    </NativeBaseProvider>
  );
}
